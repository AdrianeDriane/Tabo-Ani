import { AuthAccessCheckPage } from "../../features/auth";

// TODO(auth-check): Remove this temporary page wrapper when the /auth-check route is removed.
export function AuthAccessCheck() {
  return <AuthAccessCheckPage />;
}
